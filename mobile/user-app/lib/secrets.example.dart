/// Copy this file to `secrets.dart` and fill in real values.
/// `secrets.dart` is gitignored — NEVER commit real keys.
const supabaseUrl = 'https://YOUR-PROJECT-REF.supabase.co';
const supabaseAnonKey = 'YOUR-ANON-KEY';

/// CryptoClock Pro Hub base URL (server) — your Mac's LAN IP for local dev.
const hubBaseUrl = 'http://192.168.1.39:4000';

/// Google OAuth **Web** client ID (Google Cloud → Credentials). Used as
/// serverClientId for native Google Sign-In on mobile. Public value.
const googleWebClientId = 'YOUR-WEB-CLIENT-ID.apps.googleusercontent.com';
